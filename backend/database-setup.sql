-- FoodieHub Database Setup for XAMPP
-- Run this in phpMyAdmin or MySQL command line

-- Create the database
CREATE DATABASE IF NOT EXISTS `foodiehub` 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- Use the database
USE `foodiehub`;

-- Show confirmation
SELECT 'Database foodiehub created successfully!' as message;
